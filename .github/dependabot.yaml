version: 2
updates:
  # Enable version updates for pnpm
  - package-ecosystem: "pnpm"
    # Look for `pnpm-lock.yaml` files in the `root` directory
    directory: "/"
    # Check the npm registry for updates every day (weekdays)
    schedule:
      interval: "weekly"
    versioning-strategy: increase-if-necessary
